# Swing Calculator

This is a simple GUI calculator. It uses Swing because Swing comes with the regular JDK, so the project compiles without setting up JavaFX.

## Concepts

- GUI layout
- buttons
- event listeners
- lambdas
- UI state

## Run

```bash
javac -d out src/*.java
java -cp out CalculatorApp
```
