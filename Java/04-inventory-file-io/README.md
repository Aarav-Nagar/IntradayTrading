# Inventory Management with File I/O

This project reads products from a CSV file, lets the user adjust quantities, and saves changes back to disk.

## Concepts

- file input/output
- parsing strings
- exceptions
- classes
- `ArrayList`

## Run

```bash
javac -d out src/*.java
java -cp out InventoryApp
```
