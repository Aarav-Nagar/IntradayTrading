# To-Do List UI

This is a small GUI task manager. It can add tasks, mark them complete, and delete selected tasks.

## Concepts

- Swing controls
- `DefaultListModel`
- event listeners
- object display with `toString`
- simple UI state

## Run

```bash
javac -d out src/*.java
java -cp out TodoListApp
```
