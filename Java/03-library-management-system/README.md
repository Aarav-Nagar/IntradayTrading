# Library Management System

This project models a small library with books and media items. A `Library` object owns the collection, which is an example of object composition.

## Concepts

- object composition
- `ArrayList`
- inheritance
- searching
- checkout/return state

## Run

```bash
javac -d out src/*.java
java -cp out LibraryApp
```
